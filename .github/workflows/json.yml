# auto-generated by scripts/builds. DO NOT EDIT.
name: json

on:
  push:
    branches:
      - main
  pull_request:
    paths:
      - json/**
      - .github/workflows/json.yml

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: ./json
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: ./json/go.mod
          cache: true
          cache-dependency-path: ./json/go.sum
      - run: go build -v ./...
      - run: go test -race -v ./...
