# auto-generated by scripts/dependabot. DO NOT EDIT.
name: xpty

on:
  push:
    branches:
      - main
  pull_request:
    paths:
      - xpty/**
      - .github/workflows/xpty.yml

jobs:
  build:
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    defaults:
      run:
        working-directory: ./xpty
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: ./xpty/go.mod
          cache: true
          cache-dependency-path: ./xpty/go.sum
      - run: go build -v ./...
      - run: go test -race -v ./...
